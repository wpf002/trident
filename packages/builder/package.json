{
  "name": "@trident/builder",
  "version": "1.0.0",
  "description": "Autonomous coding agent for Trident — plan, write, test, commit inside a sandbox.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "@trident/builder-runtime": "*",
    "@trident/core": "*",
    "better-sqlite3": "^12.9.0",
    "nanoid": "^5.0.7",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}
