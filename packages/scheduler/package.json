{
  "name": "@trident/scheduler",
  "version": "1.0.0",
  "description": "Scheduled chain runner for Trident — reads schedules.json and runs chains on cron",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@trident/core": "1.0.0",
    "better-sqlite3": "^12.9.0",
    "node-cron": "^3.0.3",
    "nanoid": "^5.0.7",
    "chalk": "^5.3.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.11.5",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.3.3"
  }
}
